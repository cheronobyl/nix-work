# nix
A more generic nix repo

A basic breakdown of the structure:
hosts: Configuration specific to individual hosts
modules: Configuration shared between many machines, broken down by operating system, purpose, and other determining factors